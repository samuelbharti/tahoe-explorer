*.Rproj
.Rproj.user
.Rhistory
.RData
.Ruserdata

# Local config/secrets
.Renviron

# Google Cloud / Vertex credentials — never commit; the app uses gcloud ADC
application_default_credentials.json
gha-creds-*.json
*-sa.json
*-service-account.json

# renv local library/cache (keep renv.lock tracked)
renv/library/
renv/staging/

# Local development helpers
dev/.Rhistory

# Downloaded Tahoe metadata — never commit real data (only synthetic fixtures)
/data/**
!/data/.gitkeep
!/data/fixtures/
!/data/fixtures/**

# Local working docs and tooling (not committed)
docs/plans/
.claude/

# Scratch / temp
*.local
scratch/
.DS_Store
