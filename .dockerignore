.git
.gitignore
.github
.Rproj.user
.Rhistory
.RData
.Ruserdata
renv/library
renv/staging

# Real Tahoe metadata -- never bake into the image (only synthetic fixtures)
data/*
!data/fixtures/
!data/fixtures/**

dev
docs
tests
*.Rproj

# Real Tahoe metadata -- never bake into the image (only the synthetic fixtures
# ship, so the container runs the offline demo). Mirrors .gitignore.
data/*
!data/fixtures/
!data/fixtures/**

# Dev tooling / configs not needed in the image
template.yml
.pre-commit-config.yaml
air.toml
.lintr
.markdownlint.json
.markdownlintignore
.Renviron
.Renviron.example
CONTRIBUTING.md
CODE_OF_CONDUCT.md
CHANGELOG.md
